<%= form_with(model: Topick.new, local: true) do |form| %>
    <div class="topick_content">
        <%= form.label :content %>
        <%= form.text_field :content %>
    </div>
    <%= form.submit %>
<% end %>