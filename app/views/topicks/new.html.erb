<%= form_with(model: @topick, local: true) do |form| %>
    <div class="topick_content">
        <%= form.label :content %>
        <%= form.text_field :content %>
    </div>
    <%= form.submit %>
<% end %>