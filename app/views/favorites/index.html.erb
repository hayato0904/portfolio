<div class="container">
<table class = "table table-condensed">
    <tr>
        <th class= "active">投稿内容</th>
    </tr>
    <% @favorites.each do |favorite| %>
        <tr>
            <td class = "success"><%= favorite.topick.content %></td>
        </tr>
        <% end %>
        </table>
        <%= link_to '掲示板一覧画面に戻る', topicks_path %>
        </div>